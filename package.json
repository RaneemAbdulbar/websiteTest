{
  "name": "website",
  "version": "1.0.0",
  "description": "* `git status` tells whether something is commited or not  * `git add` marks up for commitment  * `git add .` marks up everything for commitment  * `git commit -m \"here is the message\"` commits  * `git push` pushes into remote repository",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.8"
  }
}
